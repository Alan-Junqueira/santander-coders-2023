import { DateAfterNowDirective } from './date-after-now.directive';

describe('DateAfterNowDirective', () => {
  it('should create an instance', () => {
    const directive = new DateAfterNowDirective();
    expect(directive).toBeTruthy();
  });
});
