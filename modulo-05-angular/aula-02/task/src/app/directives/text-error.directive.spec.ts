import { TextErrorDirective } from './text-error.directive';

describe('TextErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextErrorDirective();
    expect(directive).toBeTruthy();
  });
});
